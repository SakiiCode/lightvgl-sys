[package]
name = "lvgl-sys"
version = "9.2.2"
edition = "2021"

[dependencies]

[build-dependencies]
bindgen = "0.71.0"
cc = "1.2"

[features]
default = ["library", "raw-bindings"]
library = []
raw-bindings = []
